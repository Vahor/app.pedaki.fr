{
  "name": "@pedaki/auth",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "publishConfig": {
    "directory": "dist"
  },
  "scripts": {
    "lint": "eslint .",
    "format:check": "prettier --check \"**/*.{ts,tsx,mdx}\" --cache",
    "format": "prettier --write \"**/*.{ts,tsx,mdx}\" --cache",
    "typecheck": "tsc --noEmit",

    "build": "tsup"
  },
  "devDependencies": {
    "@pedaki/db": "workspace:*",
    "@pedaki/esbuild-plugin-tsconfig-paths": "workspace:*"
  },
  "prettier": "@pedaki/prettier-config"
}
